Subject: [PATCH] clk: provide Linux-observed ST1010 scanout clock

The Linux driver advertises the panel's 159.42 MHz mode to DSIM, while the
i.MX8MM clock tree rounds the actual LCDIF scanout clock to 148.5 MHz. Keep
the 159.42 MHz display timing for DSIM packet calculation, but provide a
594 MHz VIDEO_PLL1 parent so LCDIF uses the same observed 148.5 MHz rate.

Upstream-Status: Inappropriate [Jaguar Screen panel configuration]

diff --git a/arch/arm/dts/imx8mm-evk-u-boot.dtsi b/arch/arm/dts/imx8mm-evk-u-boot.dtsi
--- a/arch/arm/dts/imx8mm-evk-u-boot.dtsi
+++ b/arch/arm/dts/imx8mm-evk-u-boot.dtsi
@@ -186,0 +187,11 @@
+
+&lcdif {
+	assigned-clocks = <&clk IMX8MM_VIDEO_PLL1>,
+			  <&clk IMX8MM_CLK_LCDIF_PIXEL>,
+			  <&clk IMX8MM_CLK_DISP_AXI>,
+			  <&clk IMX8MM_CLK_DISP_APB>;
+	assigned-clock-parents = <0>, <&clk IMX8MM_VIDEO_PLL1_OUT>,
+				 <&clk IMX8MM_SYS_PLL2_1000M>,
+				 <&clk IMX8MM_SYS_PLL1_800M>;
+	assigned-clock-rates = <594000000>, <148500000>, <500000000>, <200000000>;
+};
