# DT510 — KSZ9896 DSA: end0 is the CPU/master netdev (leave unmanaged).
# WAN: DHCP on lan1 — DT510-wan.
# LAN: bridge br-lan with lan2–lan4 — ipv4.method shared (NAT + DHCP for downstream hosts).
[device]
match-device=interface-name:end0
managed=false
