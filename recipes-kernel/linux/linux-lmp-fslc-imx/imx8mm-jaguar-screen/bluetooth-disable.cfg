# Active Screen has no product Bluetooth function.  Disable the subsystem so
# neither the inherited UART serdev device nor a USB Bluetooth interface can
# create an HCI controller.
# CONFIG_BT is not set
