# TI BQ25792 — DT has ti,bq25792@6b + simple-battery (see imx8mm-jaguar-dt510.dts).
# When MACHINE_FEATURES contains bq25792-charger: 0010-mfd-bq257xx-add-bq25703a-core-fslc.patch
# and bq257xx-mfd-kconfig.cfg add CONFIG_MFD_BQ257XX=m (MFD core only).
# CONFIG_CHARGER_BQ257XX and ti,bq25792 still require further driver backports.
# Lab: CONFIG_I2C_CHARDEV from i2c-dev-interface.cfg — use i2cget/i2cdetect on i2c-2 (I2C3).
#
# --- Factory kernel identity (important; pin from your build, not from docs alone) ---
# Recipe: linux-lmp-fslc-imx. Upstream tree: Freescale/linux-fslc, branch 6.6-2.2.x-imx.
# Exact commit: meta-lmp SRCREV_machine — run:  bitbake -e linux-lmp-fslc-imx | grep '^SRCREV'
# Example branch tip checked 2026-04-14: 97812d71 (LINUX_VERSION 6.6.129) — yours may differ.
#
# Patch apply check (2026-04-14): Freescale/linux-fslc branch 6.6-2.2.x-imx has NO
# drivers/mfd/bq257xx.c until backported. Mainline prerequisite: commit 3b1bbfb5fce3
# ("mfd: bq257xx: Add support for BQ25703A"). Cherry-pick test onto 97812d71: mostly
# applies; drivers/mfd/Makefile needs manual conflict resolution (add bq257xx obj line).
# The lore “[v6] Add support for TI BQ25792” series is a delta ON TOP of that stack.
# You cannot apply the BQ25792 series alone: first land bq257xx MFD + charger + regulator
# + include/linux/mfd/bq257xx.h + Kconfig/Makefile (+ power-supply/regulator pieces from
# that mainline merge), then apply BQ25792 patches. Track: GitHub issue #3.
#
# When the factory kernel has bq257xx + BQ25792 support, add:
# CONFIG_MFD_BQ257XX=m
# CONFIG_CHARGER_BQ257XX=m
