[Manager]
# systemd-shutdown has already unmounted and synced storage before this timer
# is armed. Reset promptly if the inherited display pipeline blocks the kernel
# device-shutdown walk before the i.MX restart handler is reached.
RebootWatchdogSec=2s
