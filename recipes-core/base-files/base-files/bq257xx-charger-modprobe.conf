# BQ25792: MFD (drivers/mfd/bq257xx.ko) registers child platform devices;
# the charger/power_supply driver is separate (drivers/power/supply/bq257xx_charger.ko).
# Without this, udev only loads bq257xx and charger probe never runs.
softdep bq257xx post: bq257xx_charger
